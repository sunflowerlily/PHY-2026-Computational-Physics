import numpy as np

def forward_diff(f, x, h):
    """
    计算前向差分导数: f'(x) ≈ (f(x+h) - f(x)) / h
    TODO: 学生需实现
    """
    pass

def central_diff(f, x, h):
    """
    计算中心差分导数: f'(x) ≈ (f(x+h) - f(x-h)) / 2h
    TODO: 学生需实现
    """
    pass
