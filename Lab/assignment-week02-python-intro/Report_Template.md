# Week 02 Lab Report: AI Code Review

**Team Name**: [例如：Team_01_05]

**Members**:
1. [姓名] ([学号]) - 负责任务: [例如：Part 1 Bug复现]
2. [姓名] ([学号]) - 负责任务: [例如：Part 1 Code Review]
3. [姓名] ([学号]) - 负责任务: [例如：Part 1 重构 + Part 2]

---

## Part 1: Core Task - 捉鬼特工队

### 1. Bug 现象记录
请描述运行 `notebook_review.ipynb` 时观察到的 3 个诡异现象：
1.  **现象 1**: [例如：粒子速度莫名其妙变快]
2.  **现象 2**: [例如：只移动了 p2，但 p1 也跟着动了]
3.  **现象 3**: [例如：运动方向和预期的 45 度不符]

### 2. Code Review (陷阱分析)
请指出 `bad_particle.py` 中的具体错误代码行，并解释原因：

*   **陷阱 1 (单位混淆)**:
    *   错误代码: `...`
    *   原因分析: `math.sin` 接收的是弧度还是角度？

*   **陷阱 2 (浅拷贝灾难)**:
    *   错误代码: `p2 = p1`
    *   原因分析: 为什么修改 `p2` 会影响 `p1`？这是 Python 的什么特性？

*   **陷阱 3 (列表与类型)**:
    *   错误代码: `self.pos = [x, y]`
    *   原因分析: 为什么物理计算不推荐用 list？

### 3. AI 交互记录 (Mandatory)
> **教学目标**：学会如何向 AI 提问以发现隐蔽 Bug。

*   **我的 Prompt**:
    ```text
    [请粘贴你发给 AI 的原始 Prompt，例如："这段 Python 代码有什么物理错误？"]
    ```

*   **AI 的回答摘要**:
    *   AI 是否发现了浅拷贝问题？ (是/否)
    *   AI 是否发现了角度单位问题？ (是/否)

---

## Part 2: Bonus Task - 矢量化狂魔

### 1. 性能对比
请记录在 100 个粒子（或更多）下的运行时间：

| 方法 | 耗时 (ms) |
| :--- | :--- |
| **Python Loops (math)** | `0.00` |
| **Numpy Vectorized** | `0.00` |
| **Speedup (加速比)** | `0.0` x |

### 2. 核心代码展示
请展示你实现“一步更新所有粒子位置”的关键代码行（不要粘贴全部代码）：
```python
# [粘贴你的核心矢量化代码]
# self.pos += ...
```
